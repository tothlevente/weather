import { LocateFixedIcon } from "lucide-react";
import { Button } from "./ui/button";

export function LocateUser() {
  return (
    <Button
      size="icon"
      variant="outline"
    >
      <LocateFixedIcon />
    </Button>
  );
}
